# Name Test

A mobile-only Svelte + Vite application for name selection and letter matching.

## Setup

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

## Deploy

The project is configured to deploy to GitHub Pages automatically via GitHub Actions when pushing to the `main` branch.

